name: image_composer
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pillow
  - numpy
  - pip:
    - tkinterdnd2   # Windows拖拽支持（更稳定）
    - windnd        # 备选拖拽方案
    - pyinstaller   # 用于打包
